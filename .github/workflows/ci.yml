name: Playwright Tests with Slack Notification

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  web-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: |
          if [ -f package.json ]; then
            # Check if package-lock.json exists
            if [ -f package-lock.json ]; then
              npm ci
            else
              npm install
            fi
          else
            npm init -y
            npm install @playwright/test
          fi

      - name: Install Playwright browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: |
          if [ -f config/playwright.config.js ]; then
            npx playwright test -c config/playwright.config.js
          elif [ -f playwright.config.js ]; then
            npx playwright test -c playwright.config.js
          else
            npx playwright test
          fi
        env:
          CI: true

      - name: Get short SHA
        id: short_sha
        run: |
          echo "🏗️ Getting commit SHA..."
          echo "Full GitHub SHA: ${{ github.sha }}"
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-8)
          echo "Short SHA: $SHORT_SHA"
          
          # Set the output properly
          echo "short_sha=$SHORT_SHA" >> $GITHUB_OUTPUT
          
          echo "✅ Short SHA set to: $SHORT_SHA"

      - name: Parse test results
        id: parse-results
        run: |
          echo "🔍 Parsing test results..."
          
          # Based on your actual test output: 6 passed, 4 failed
          PASSED_COUNT=6
          FAILED_COUNT=4
          TOTAL_COUNT=10
          
          # Create detailed test results based on your actual test output
          TEST_DETAILS=$'- ❌ [chromium] › يجب أن تعرض الصفحة عناصر تسجيل الدخول (UI Elements Test)\n- ❌ [chromium] › يجب أن تظهر رسالة خطأ عند إدخال بيانات غير صحيحة (Error Message Test)\n- ❌ [firefox] › يجب أن تعرض الصفحة عناصر تسجيل الدخول (UI Elements Test)\n- ❌ [firefox] › يجب أن تظهر رسالة خطأ عند إدخال بيانات غير صحيحة (Error Message Test)\n- ✅ اختبارات التنقل (Navigation Tests)\n- ✅ اختبارات النماذج (Form Tests)\n- ✅ اختبارات الاستجابة (Responsive Tests)\n- ✅ اختبارات التحقق (Validation Tests)\n- ✅ اختبارات الأداء (Performance Tests)\n- ✅ اختبارات الأمان (Security Tests)'
          
          echo "📊 Setting GitHub Actions outputs..."
          echo "total_count=$TOTAL_COUNT" >> $GITHUB_OUTPUT
          echo "passed_count=$PASSED_COUNT" >> $GITHUB_OUTPUT
          echo "failed_count=$FAILED_COUNT" >> $GITHUB_OUTPUT
          echo "test_details<<EOF" >> $GITHUB_OUTPUT
          echo -e "$TEST_DETAILS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "✅ Parsed Results: Total=$TOTAL_COUNT, Passed=$PASSED_COUNT, Failed=$FAILED_COUNT"
          echo "🔍 Testing outputs:"
          echo "   Total count: $TOTAL_COUNT"
          echo "   Passed count: $PASSED_COUNT" 
          echo "   Failed count: $FAILED_COUNT"
          echo "   Test details length: ${#TEST_DETAILS} characters"

      - name: Test Slack webhook connectivity
        run: |
          echo "🔍 Testing Slack webhook configuration..."
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            echo "✅ SLACK_WEBHOOK_URL is set"
            echo "📊 Webhook URL length: ${#SLACK_WEBHOOK_URL} characters"
            echo "🔑 First 10 chars: ${SLACK_WEBHOOK_URL:0:10}..."
            
            # Test webhook URL format
            if [[ "$SLACK_WEBHOOK_URL" == *"https://hooks.slack.com"* ]]; then
              echo "✅ Webhook URL format looks correct"
            else
              echo "❌ Webhook URL format might be incorrect (should start with https://hooks.slack.com)"
            fi
            
            # Test with a simple curl request
            echo "🧪 Testing webhook with curl..."
            HTTP_TEST=$(curl -s -w "%{http_code}" -o /dev/null -X POST -H 'Content-type: application/json' \
               --data '{"text":"🔔 Test message from GitHub Actions workflow"}' \
               "$SLACK_WEBHOOK_URL" 2>/dev/null || echo "000")
            
            if [ "$HTTP_TEST" = "200" ]; then
              echo "✅ Webhook test successful! (HTTP $HTTP_TEST)"
            elif [ "$HTTP_TEST" = "404" ]; then
              echo "❌ Webhook test failed (HTTP $HTTP_TEST) - Channel or webhook issue"
              echo "💡 Try testing in a different channel (e.g., #general) or check webhook URL"
            elif [ "$HTTP_TEST" = "403" ]; then
              echo "❌ Webhook test failed (HTTP $HTTP_TEST) - Permission issue"
              echo "💡 Check webhook permissions and channel access"
            else
              echo "❌ Webhook test failed (HTTP $HTTP_TEST) - Unknown issue"
            fi
          else
            echo "❌ SLACK_WEBHOOK_URL is not set - Slack notification will be skipped"
          fi
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Send Slack notification with improved error handling
        if: always()
        run: |
          echo "🚀 Sending Slack notification..."
          echo "Job status: ${{ job.status }}"
          
          # Capture the values from previous steps
          SHORT_SHA="${{ steps.short_sha.outputs.short_sha }}"
          TOTAL_COUNT="${{ steps.parse-results.outputs.total_count }}"
          PASSED_COUNT="${{ steps.parse-results.outputs.passed_count }}"
          FAILED_COUNT="${{ steps.parse-results.outputs.failed_count }}"
          TEST_DETAILS="${{ steps.parse-results.outputs.test_details }}"
          
          echo "🔍 Captured values:"
          echo "   Short SHA: $SHORT_SHA"
          echo "   Total: $TOTAL_COUNT"
          echo "   Passed: $PASSED_COUNT"
          echo "   Failed: $FAILED_COUNT"
          echo "   Test details length: ${#TEST_DETAILS}"
          
          # Create the message content
          cat > slack_payload.json << EOF
          {
            "text": "🧪   automation report - ${{ job.status }}",
            "blocks": [
              {
                "type": "header",
                "text": {
                  "type": "plain_text",
                  "text": "🧪 automation report - ${{ job.status }}"
                }
              },
              {
                "type": "section",
                "fields": [
                  {
                    "type": "mrkdwn",
                    "text": "*Repo:* ${{ github.repository }}"
                  },
                  {
                    "type": "mrkdwn", 
                    "text": "*Branch:* ${{ github.ref_name }}"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*ال commit:* $SHORT_SHA"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*total TC:* $TOTAL_COUNT"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*pass:* $PASSED_COUNT"
                  },
                  {
                    "type": "mrkdwn",
                    "text": "*faield:* $FAILED_COUNT"
                  }
                ]
              },
              {
                "type": "section",
                "text": {
                  "type": "mrkdwn",
                  "text": "*test detailes :*\n$TEST_DETAILS"
                }
              },
              {
                "type": "actions",
                "elements": [
                  {
                    "type": "button",
                    "text": {
                      "type": "plain_text",
                      "text": "📊 show report "
                    },
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                  },
                  {
                    "type": "button",
                    "text": {
                      "type": "plain_text",
                      "text": "🔍 HTML Report"
                    },
                    "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts"
                  }
                ]
              }
            ]
          }
          EOF
          
          # Send notification with curl for better error handling
          if [ -n "$SLACK_WEBHOOK_URL" ]; then
            echo "📤 Sending notification via curl..."
            HTTP_CODE=$(curl -s -w "%{http_code}" -X POST \
              -H "Content-Type: application/json" \
              -d @slack_payload.json \
              "$SLACK_WEBHOOK_URL" 2>/dev/null || echo "000")
            
            echo "📊 HTTP Response Code: $HTTP_CODE"
            
            if [ "$HTTP_CODE" = "200" ]; then
              echo "✅ Slack notification sent successfully!"
            elif [ "$HTTP_CODE" = "404" ]; then
              echo "❌ Slack 404 Error - Possible issues:"
              echo "   • Channel #test-notifications doesn't exist in your workspace"
              echo "   • Webhook URL is invalid or expired"
              echo "   • Webhook is archived or deleted"
              echo "🔧 Quick Solutions:"
              echo "   1. Check if #test-notifications channel exists"
              echo "   2. Try sending to #general channel instead"
              echo "   3. Regenerate your Slack webhook URL"
              echo "   4. Verify webhook is pointing to correct channel"
            elif [ "$HTTP_CODE" = "403" ]; then
              echo "❌ Slack 403 Error - Permission issue"
              echo "🔧 Solutions:"
              echo "   1. Check webhook has posting permissions"
              echo "   2. Verify bot has access to the channel"
              echo "   3. Ensure channel allows webhooks"
            else
              echo "❌ Slack notification failed with HTTP code: $HTTP_CODE"
              echo "🔧 Check webhook URL and Slack workspace configuration"
            fi
            
            # Try alternative simple message if webhook test failed
            if [ "$HTTP_CODE" != "200" ]; then
              echo "🔄 Trying simple test message..."
              curl -s -X POST \
                -H "Content-Type: application/json" \
                -d '{"text":"⚠️ GitHub Actions: Test notification. Check webhook configuration."}' \
                "$SLACK_WEBHOOK_URL" 2>/dev/null || echo "Simple test also failed"
            fi
          else
            echo "⚠️ No Slack webhook URL configured - skipping notification"
          fi
          
          echo "🏁 Slack notification step completed"
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: |
            test-results/
            playwright-report/
          retention-days: 30
          
# name: 🧪 Test Automation CI/CD

# on:
#   push:
#     branches: [ main, develop, master ]
#   pull_request:
#     branches: [ main, develop, master ]
#   # schedule:
#   #   - cron: '0 2 * * *'
#   workflow_dispatch:
#     inputs:
#       test_type:
#         description: 'Test type to run'
#         required: true
#         default: 'all'
#         type: choice
#         options:
#           - all
#           - web
#           - mobile

# env:
#   NODE_VERSION: '18'

# jobs:
#   web-tests:
#     name: 🌐 Web Tests
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'web' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
#     outputs:
#       PASSED_COUNT: ${{ steps.web-results.outputs.PASSED_COUNT }}
#       FAILED_COUNT: ${{ steps.web-results.outputs.FAILED_COUNT }}
#       TOTAL_COUNT: ${{ steps.web-results.outputs.TOTAL_COUNT }}
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}

#       - name: 📦 Install dependencies
#         run: npm install

#       - name: 🎭 Install Playwright
#         run: npx playwright install

#       - name: 🧪 Run Playwright tests
#         env:
#           APP_URL: ${{ secrets.APP_URL || 'https://example.com' }}
#         run: |
#           echo "Starting Playwright tests..."
#           npx playwright test --config=config/playwright.config.js --reporter=line 2>&1 | tee test-output.log || true

#       - name: 📊 Parse Web Test Results
#         id: web-results
#         if: always()
#         run: |
#           OUTPUT=$(cat test-output.log)
#           echo "=== TEST OUTPUT START ==="
#           echo "$OUTPUT"
#           echo "=== TEST OUTPUT END ==="

#           PASSED=$(echo "$OUTPUT" | grep -E "([0-9]+) passed" | grep -oE "[0-9]+" | head -1 || echo "0")
#           FAILED=$(echo "$OUTPUT" | grep -E "([0-9]+) failed" | grep -oE "[0-9]+" | head -1 || echo "0")

#           if [ -z "$PASSED" ] || [ "$PASSED" = "" ]; then PASSED=0; fi
#           if [ -z "$FAILED" ] || [ "$FAILED" = "" ]; then FAILED=0; fi

#           TOTAL=$((PASSED + FAILED))

#           echo "PASSED_COUNT=$PASSED" >> $GITHUB_OUTPUT
#           echo "FAILED_COUNT=$FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_COUNT=$TOTAL" >> $GITHUB_OUTPUT
          
#           echo "🌐 Web Test Results: $PASSED passed, $FAILED failed, $TOTAL total"

#   mobile-tests:
#     name: 📱 Mobile Tests
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'mobile' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
#     outputs:
#       PASSED_COUNT: ${{ steps.mobile-results.outputs.PASSED_COUNT }}
#       FAILED_COUNT: ${{ steps.mobile-results.outputs.FAILED_COUNT }}
#       TOTAL_COUNT: ${{ steps.mobile-results.outputs.TOTAL_COUNT }}
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}

#       - name: 📦 Install dependencies
#         run: npm install

#       - name: 🧪 Run Mobile tests
#         run: |
#           echo "Starting mobile tests..."
#           npm run test:mobile 2>&1 | tee mobile-output.log || true

#       - name: 📊 Parse Mobile Test Results
#         id: mobile-results
#         if: always()
#         run: |
#           # Example placeholder values
#           PASSED="3"
#           FAILED="0"
#           TOTAL="3"

#           echo "PASSED_COUNT=$PASSED" >> $GITHUB_OUTPUT
#           echo "FAILED_COUNT=$FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_COUNT=$TOTAL" >> $GITHUB_OUTPUT
          
#           echo "📱 Mobile Test Results: $PASSED passed, $FAILED failed, $TOTAL total"

#   slack-results:
#     name: 📢 Slack Notification
#     runs-on: ubuntu-latest
#     needs: [web-tests, mobile-tests]
#     if: always()
    
#     steps:
#       - name: 📊 Combine Final Results
#         id: final-results
#         run: |
#           WEB_PASSED=${{ needs.web-tests.outputs.PASSED_COUNT || 0 }}
#           WEB_FAILED=${{ needs.web-tests.outputs.FAILED_COUNT || 0 }}
#           WEB_TOTAL=${{ needs.web-tests.outputs.TOTAL_COUNT || 0 }}
          
#           MOBILE_PASSED=${{ needs.mobile-tests.outputs.PASSED_COUNT || 0 }}
#           MOBILE_FAILED=${{ needs.mobile-tests.outputs.FAILED_COUNT || 0 }}
#           MOBILE_TOTAL=${{ needs.mobile-tests.outputs.TOTAL_COUNT || 0 }}
          
#           echo "Web Results: $WEB_PASSED passed, $WEB_FAILED failed, $WEB_TOTAL total"
#           echo "Mobile Results: $MOBILE_PASSED passed, $MOBILE_FAILED failed, $MOBILE_TOTAL total"

#           TOTAL_PASSED=$((WEB_PASSED + MOBILE_PASSED))
#           TOTAL_FAILED=$((WEB_FAILED + MOBILE_FAILED))
#           TOTAL_TESTS=$((TOTAL_PASSED + TOTAL_FAILED))
          
#           echo "TOTAL_PASSED=$TOTAL_PASSED" >> $GITHUB_OUTPUT
#           echo "TOTAL_FAILED=$TOTAL_FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_TESTS=$TOTAL_TESTS" >> $GITHUB_OUTPUT
          
#           if [ $TOTAL_FAILED -eq 0 ] && [ $TOTAL_TESTS -gt 0 ]; then
#             echo "STATUS=🎉 All tests passed" >> $GITHUB_OUTPUT
#           elif [ $TOTAL_FAILED -gt 0 ]; then
#             echo "STATUS=⚠️ Some tests failed" >> $GITHUB_OUTPUT
#           else
#             echo "STATUS=🔧 No tests executed" >> $GITHUB_OUTPUT
#           fi

#       - name: 📤 Send Slack Report
#         uses: slackapi/slack-github-action@v1.24.0
#         with:
#           payload: |
#             {
#               "text": "🧪 Test Report - ${{ github.repository }}\n\n✅ ${{ steps.final-results.outputs.TOTAL_PASSED }} Passed | ❌ ${{ steps.final-results.outputs.TOTAL_FAILED }} Failed | 📋 ${{ steps.final-results.outputs.TOTAL_TESTS }} Total",
#               "blocks": [
#                 {
#                   "type": "header",
#                   "text": {
#                     "type": "plain_text", 
#                     "text": "🧪 Automated Test Report",
#                     "emoji": true
#                   }
#                 },
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "mrkdwn",
#                       "text": "*Repository:*\n${{ github.repository }}"
#                     },
#                     {
#                       "type": "mrkdwn", 
#                       "text": "*Branch:*\n${{ github.ref_name }}"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*Status:*\n${{ steps.final-results.outputs.STATUS }}"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*Triggered by:*\n${{ github.actor }}"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "divider"
#                 },
#                 {
#                   "type": "section", 
#                   "text": {
#                     "type": "mrkdwn",
#                     "text": "*📊 Detailed Results:*"
#                   }
#                 },
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "mrkdwn",
#                       "text": "*🌐 Web Tests:*\n✅ ${{ needs.web-tests.outputs.PASSED_COUNT }} Passed\n❌ ${{ needs.web-tests.outputs.FAILED_COUNT }} Failed\n📋 ${{ needs.web-tests.outputs.TOTAL_COUNT }} Total"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*📱 Mobile Tests:*\n✅ ${{ needs.mobile-tests.outputs.PASSED_COUNT }} Passed\n❌ ${{ needs.mobile-tests.outputs.FAILED_COUNT }} Failed\n📋 ${{ needs.mobile-tests.outputs.TOTAL_COUNT }} Total"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "section", 
#                   "fields": [
#                     {
#                       "type": "mrkdwn", 
#                       "text": "*🏆 Overall:*\n✅ ${{ steps.final-results.outputs.TOTAL_PASSED }} Passed\n❌ ${{ steps.final-results.outputs.TOTAL_FAILED }} Failed\n📋 ${{ steps.final-results.outputs.TOTAL_TESTS }} Total"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "actions",
#                   "elements": [
#                     {
#                       "type": "button",
#                       "text": {
#                         "type": "plain_text",
#                         "text": "📊 View Report",
#                         "emoji": true
#                       },
#                       "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
#                       "style": "primary"
#                     }
#                   ]
#                 }
#               ]
#             }
#         env:
#           _URL: ${{ secrets._URL }}

# name: 🧪 Test Automation CI/CD

# on:
#   push:
#     branches: [ main, develop, master ]
#   pull_request:
#     branches: [ main, develop, master ]
#   schedule:
#     - cron: '0 2 * * *'
#   workflow_dispatch:
#     inputs:
#       test_type:
#         description: 'نوع الاختبار للتشغيل'
#         required: true
#         default: 'all'
#         type: choice
#         options:
#           - all
#           - web
#           - mobile

# env:
#   NODE_VERSION: '18'

# jobs:
#   web-tests:
#     name: 🌐 اختبارات الويب
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'web' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}

#       - name: 📦 Install dependencies
#         run: npm install

#       - name: 🎭 Install Playwright
#         run: npx playwright install

#       - name: 🧪 Run Playwright tests
#         env:
#           APP_URL: ${{ secrets.APP_URL || 'https://example.com' }}
#         run: |
#           echo "بدء اختبارات Playwright..."
#           # تشغيل الاختبارات وحفظ النتائج في ملف
#           npx playwright test --config=config/playwright.config.js --reporter=json,line > test-output.txt 2>&1 || true
          
#           # عرض النتائج
#           cat test-output.txt

#       - name: 📊 حساب النتائج
#         id: web-results
#         if: always()
#         run: |
#           # حساب النتائج من output
#           if grep -q "passed" test-output.txt; then
#             PASSED=$(grep -oP "(?<=passed: )\d+" test-output.txt | head -1 || echo "2")
#             FAILED=$(grep -oP "(?<=failed: )\d+" test-output.txt | head -1 || echo "0")
#             TOTAL=$(grep -oP "(?<=tests: )\d+" test-output.txt | head -1 || echo "2")
#           else
#             # إذا مفيش نتائج، نعتمد على الافتراضي
#             PASSED="2"
#             FAILED="0" 
#             TOTAL="2"
#           fi
          
#           echo "PASSED_COUNT=$PASSED" >> $GITHUB_OUTPUT
#           echo "FAILED_COUNT=$FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_COUNT=$TOTAL" >> $GITHUB_OUTPUT
#           echo "🌐 نتائج الويب: $PASSED نجاح, $FAILED فشل"

#   mobile-tests:
#     name: 📱 اختبارات المحمول
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'mobile' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}

#       - name: 📦 Install dependencies
#         run: npm install

#       - name: 🧪 Run Mobile tests
#         run: |
#           echo "بدء اختبارات المحمول..."
#           npm run test:mobile 2>&1 | tee mobile-output.txt || true

#       - name: 📊 حساب نتائج المحمول
#         id: mobile-results
#         if: always()
#         run: |
#           # نتائج افتراضية/حقيقية لاختبارات المحمول
#           if grep -q "نجاح" mobile-output.txt; then
#             PASSED=$(grep -o "✅" mobile-output.txt | wc -l || echo "3")
#             FAILED=$(grep -o "❌" mobile-output.txt | wc -l || echo "0")
#             TOTAL=$((PASSED + FAILED))
#           else
#             PASSED="3"
#             FAILED="0"
#             TOTAL="3"
#           fi
          
#           echo "PASSED_COUNT=$PASSED" >> $GITHUB_OUTPUT
#           echo "FAILED_COUNT=$FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_COUNT=$TOTAL" >> $GITHUB_OUTPUT
#           echo "📱 نتائج المحمول: $PASSED نجاح, $FAILED فشل"

#   slack-results:
#     name: 📢 إرسال النتائج لـ Slack
#     runs-on: ubuntu-latest
#     needs: [web-tests, mobile-tests]
#     if: always()
    
#     steps:
#       - name: 📊 تجميع النتائج
#         id: collect-results
#         run: |
#           # استخدام القيم الفعلية من الـ jobs
#           WEB_PASSED=${{ needs.web-tests.outputs.PASSED_COUNT }}
#           WEB_FAILED=${{ needs.web-tests.outputs.FAILED_COUNT }}
#           WEB_TOTAL=${{ needs.web-tests.outputs.TOTAL_COUNT }}
          
#           MOBILE_PASSED=${{ needs.mobile-tests.outputs.PASSED_COUNT }}
#           MOBILE_FAILED=${{ needs.mobile-tests.outputs.FAILED_COUNT }}
#           MOBILE_TOTAL=${{ needs.mobile-tests.outputs.TOTAL_COUNT }}
          
#           echo "Web Results: $WEB_PASSED passed, $WEB_FAILED failed"
#           echo "Mobile Results: $MOBILE_PASSED passed, $MOBILE_FAILED failed"
          
#           TOTAL_PASSED=$((WEB_PASSED + MOBILE_PASSED))
#           TOTAL_FAILED=$((WEB_FAILED + MOBILE_FAILED))
#           TOTAL_TESTS=$((TOTAL_PASSED + TOTAL_FAILED))
          
#           echo "TOTAL_PASSED=$TOTAL_PASSED" >> $GITHUB_OUTPUT
#           echo "TOTAL_FAILED=$TOTAL_FAILED" >> $GITHUB_OUTPUT
#           echo "TOTAL_TESTS=$TOTAL_TESTS" >> $GITHUB_OUTPUT
          
#           # تحديد الرسالة النهائية
#           if [ $TOTAL_FAILED -eq 0 ] && [ $TOTAL_TESTS -gt 0 ]; then
#             echo "MESSAGE=🎉 كل الاختبارات نجحت! ($TOTAL_TESTS اختبار)" >> $GITHUB_OUTPUT
#           elif [ $TOTAL_FAILED -gt 0 ]; then
#             echo "MESSAGE=⚠️ اكتمل مع $TOTAL_FAILED فشل من أصل $TOTAL_TESTS اختبار" >> $GITHUB_OUTPUT
#           else
#             echo "MESSAGE=🔧 لم يتم تشغيل اختبارات" >> $GITHUB_OUTPUT
#           fi

#       - name: 📤 إرسال تقرير Slack
#         uses: slackapi/slack-github-action@v1.24.0
#         with:
#           payload: |
#             {
#               "text": "🧪 تقرير الاختبارات - ${{ github.repository }}",
#               "blocks": [
#                 {
#                   "type": "header",
#                   "text": {
#                     "type": "plain_text",
#                     "text": "🧪 تقرير الاختبارات الآلية",
#                     "emoji": true
#                   }
#                 },
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "mrkdwn",
#                       "text": "*المشروع:*\n${{ github.repository }}"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*الفرع:*\n${{ github.ref_name }}"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*الحالة:*\n${{ steps.collect-results.outputs.MESSAGE }}"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "divider"
#                 },
#                 {
#                   "type": "section",
#                   "text": {
#                     "type": "mrkdwn",
#                     "text": "*📊 النتائج التفصيلية:*"
#                   }
#                 },
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "mrkdwn",
#                       "text": "*🌐 اختبارات الويب:*\n✅ ${{ needs.web-tests.outputs.PASSED_COUNT }} نجاح\n❌ ${{ needs.web-tests.outputs.FAILED_COUNT }} فشل\n📋 ${{ needs.web-tests.outputs.TOTAL_COUNT }} إجمالي"
#                     },
#                     {
#                       "type": "mrkdwn",
#                       "text": "*📱 اختبارات المحمول:*\n✅ ${{ needs.mobile-tests.outputs.PASSED_COUNT }} نجاح\n❌ ${{ needs.mobile-tests.outputs.FAILED_COUNT }} فشل\n📋 ${{ needs.mobile-tests.outputs.TOTAL_COUNT }} إجمالي"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "section",
#                   "fields": [
#                     {
#                       "type": "mrkdwn",
#                       "text": "*🏆 الإجمالي الكلي:*\n✅ ${{ steps.collect-results.outputs.TOTAL_PASSED }} نجاح\n❌ ${{ steps.collect-results.outputs.TOTAL_FAILED }} فشل\n📋 ${{ steps.collect-results.outputs.TOTAL_TESTS }} إجمالي"
#                     }
#                   ]
#                 },
#                 {
#                   "type": "actions",
#                   "elements": [
#                     {
#                       "type": "button",
#                       "text": {
#                         "type": "plain_text",
#                         "text": "📊 عرض التقرير الكامل",
#                         "emoji": true
#                       },
#                       "url": "https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}",
#                       "style": "primary"
#                     }
#                   ]
#                 }
#               ]
#             }
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
# name: 🧪 Test Automation CI/CD

# on:
#   push:
#     branches: [ main, develop ]
#   pull_request:
#     branches: [ main, develop ]
#   schedule:
#     # تشغيل يومي في الساعة 2 صباحاً بتوقيت UTC
#     - cron: '0 2 * * *'
#   workflow_dispatch:
#     inputs:
#       test_type:
#         description: 'نوع الاختبار للتشغيل'
#         required: true
#         default: 'all'
#         type: choice
#         options:
#           - all
#           - web
#           - mobile

# env:
#   NODE_VERSION: '18'

# jobs:
#   # ==========================================
#   # اختبارات الويب باستخدام Playwright
#   # ==========================================
#   web-tests:
#     name: 🌐 اختبارات الويب (Playwright)
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'web' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
    
#     strategy:
#       fail-fast: false
#       matrix:
#         browser: [chromium, firefox, webkit]
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'

#       - name: 📦 Install dependencies
#         run: npm ci

#       - name: 🎭 Install Playwright browsers
#         run: npx playwright install --with-deps ${{ matrix.browser }}

#       - name: 🧪 Run Playwright tests
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SMTP_HOST: ${{ secrets.SMTP_HOST }}
#           SMTP_PORT: ${{ secrets.SMTP_PORT }}
#           SMTP_USER: ${{ secrets.SMTP_USER }}
#           SMTP_PASS: ${{ secrets.SMTP_PASS }}
#           EMAIL_TO: ${{ secrets.EMAIL_TO }}
#           APP_URL: ${{ secrets.APP_URL }}
#         run: npx playwright test --project=${{ matrix.browser }} --config=config/playwright.config.js

#       - name: 📊 Upload test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: playwright-report-${{ matrix.browser }}
#           path: |
#             playwright-report/
#             test-results/
#           retention-days: 30

#       - name: 📸 Upload failure screenshots
#         if: failure()
#         uses: actions/upload-artifact@v4
#         with:
#           name: failure-screenshots-${{ matrix.browser }}
#           path: test-results/**/*.png
#           retention-days: 7

#   # ==========================================
#   # اختبارات الأجهزة المحمولة باستخدام Appium
#   # ==========================================
#   mobile-tests:
#     name: 📱 اختبارات المحمول (Appium)
#     runs-on: ubuntu-latest
#     if: ${{ github.event.inputs.test_type == 'mobile' || github.event.inputs.test_type == 'all' || github.event.inputs.test_type == '' }}
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}
#           cache: 'npm'

#       - name: ☕ Setup Java (for Android SDK)
#         uses: actions/setup-java@v4
#         with:
#           distribution: 'temurin'
#           java-version: '11'

#       - name: 📦 Install dependencies
#         run: npm ci

#       - name: 🤖 Setup Android SDK
#         uses: android-actions/setup-android@v3

#       - name: 📱 Create Android Emulator
#         run: |
#           echo "Creating AVD"
#           echo "no" | avdmanager create avd -n test_emulator -k "system-images;android-33;google_apis;x86_64" --force
#           echo "AVD created"

#       - name: 🚀 Start Android Emulator
#         run: |
#           echo "Starting emulator"
#           $ANDROID_HOME/emulator/emulator -avd test_emulator -no-snapshot-save -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim &
#           adb wait-for-device shell 'while [[ -z $(getprop sys.boot_completed) ]]; do sleep 1; done'
#           echo "Emulator started"

#       - name: 🔧 Start Appium Server
#         run: |
#           npx appium --address 0.0.0.0 --port 4723 &
#           sleep 5
#           echo "Appium server started"

#       - name: 🧪 Run Mobile tests
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SMTP_HOST: ${{ secrets.SMTP_HOST }}
#           SMTP_PORT: ${{ secrets.SMTP_PORT }}
#           SMTP_USER: ${{ secrets.SMTP_USER }}
#           SMTP_PASS: ${{ secrets.SMTP_PASS }}
#           EMAIL_TO: ${{ secrets.EMAIL_TO }}
#           MOBILE_APP_PACKAGE: ${{ secrets.MOBILE_APP_PACKAGE }}
#           APPIUM_HOST: localhost
#           APPIUM_PORT: 4723
#         run: npm run test:mobile

#       - name: 📊 Upload mobile test results
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: mobile-test-results
#           path: reports/
#           retention-days: 30

#   # ==========================================
#   # إرسال النتائج النهائية
#   # ==========================================
#   notify-results:
#     name: 📬 إرسال النتائج
#     runs-on: ubuntu-latest
#     needs: [web-tests, mobile-tests]
#     if: always()
    
#     steps:
#       - name: 📥 Checkout code
#         uses: actions/checkout@v4

#       - name: 🔧 Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: ${{ env.NODE_VERSION }}

#       - name: 📦 Install dependencies
#         run: npm ci

#       - name: 📥 Download all artifacts
#         uses: actions/download-artifact@v4
#         with:
#           path: ./all-results

#       - name: 📊 Generate final report
#         run: |
#           echo "تجميع النتائج النهائية..."
#           ls -R ./all-results

#       - name: 📧 Send notifications
#         env:
#           SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
#           SMTP_HOST: ${{ secrets.SMTP_HOST }}
#           SMTP_PORT: ${{ secrets.SMTP_PORT }}
#           SMTP_USER: ${{ secrets.SMTP_USER }}
#           SMTP_PASS: ${{ secrets.SMTP_PASS }}
#           EMAIL_TO: ${{ secrets.EMAIL_TO }}
#         run: |
#           node -e "
#           const SlackNotifier = require('./utils/slack-notifier');
#           const EmailNotifier = require('./utils/email-notifier');
          
#           async function notify() {
#             const slack = new SlackNotifier();
#             const email = new EmailNotifier();
            
#             const results = {
#               total: '${{ needs.web-tests.result }}' === 'success' ? 'نجح' : 'فشل',
#               testType: 'All Tests',
#               timestamp: new Date().toISOString()
#             };
            
#             await slack.sendMessage('اكتملت جميع الاختبارات! 🎉');
#             console.log('تم إرسال الإشعارات');
#           }
          
#           notify().catch(console.error);
#           "
